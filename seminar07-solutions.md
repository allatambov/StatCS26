## Семинар 7

a. Собственные значения матрицы: $1 \pm 0.6$, $\lambda_1 = 1.6$, $\lambda_2 = 0.4$

b. Информативность – доля объясненной дисперсии: 

$I(PC_1) = \frac{\lambda_1}{\lambda_1+\lambda_2} = \frac{\lambda_1}{k} = \frac{1.6}{2} = 0.8$

$I(PC_2) = \frac{0.4}{2} = 0.2$

c. Собственные векторы длины 1: $v_1 = (\frac{1}{\sqrt{2}}, \frac{1}{\sqrt{2}})$ и $v_2 = (-\frac{1}{\sqrt{2}}, \frac{1}{\sqrt{2}})$.

Знаки могут быть выбраны иные, но тут важно, чтобы при записи в матрицу перехода 
они были согласованы как в матрице поворота (первый знак – поворот против часовой стрелки, второй – по часовой):

$$
P = \begin{bmatrix}
\cos\alpha & \mp\sin\alpha\\
\pm\sin\alpha & \cos\alpha
\end{bmatrix}
$$

Матрица перехода от нового базиса к старому:

$$
P = \begin{bmatrix}
\frac{1}{\sqrt{2}} & -\frac{1}{\sqrt{2}}\\
\frac{1}{\sqrt{2}} & \frac{1}{\sqrt{2}}
\end{bmatrix}
$$

В этой матрице по столбцам записаны стандартизованные факторные нагрузки, 
для получения обычных – домножаем их на корни из соответствующих собственных значений:

$$
A = \begin{bmatrix}
\frac{\sqrt{1.6}}{\sqrt{2}} & -\frac{\sqrt{0.4}}{\sqrt{2}}\\
\frac{\sqrt{1.6}}{\sqrt{2}} & \frac{\sqrt{0.4}}{\sqrt{2}}
\end{bmatrix}
$$

Теперь сумма квадратов элементов по столбцам равна собственным значениям, по строкам – 1.
Это и есть матрица факторных нагрузок.

 e. Подставляем `x = 4` и `y = -0.5` в линейные комбинации:

 $$
 PC_1 = \frac{\sqrt{1.6}}{\sqrt{2}} x + \frac{\sqrt{1.6}}{\sqrt{2}} y = 
 \frac{\sqrt{1.6}}{\sqrt{2}} \times 4 + \frac{\sqrt{1.6}}{\sqrt{2}} \times (-0.5) = 3.13
 $$

  $$
 PC_2 = -\frac{\sqrt{1.6}}{\sqrt{2}} x + \frac{\sqrt{1.6}}{\sqrt{2}} y = 
 -\frac{\sqrt{1.6}}{\sqrt{2}} \times 4 + \frac{\sqrt{1.6}}{\sqrt{2}} \times (-0.5) = -4.02
 $$




